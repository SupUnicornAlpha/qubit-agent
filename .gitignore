node_modules/
.tmp/
.tmp-*/
.tmp-test-home/
.tmp-pack-test-data/
.tmp-workflow-list-perf/
.tmp-broker-test-home/
dist/
*.sqlite
*.duckdb
*.lance/
~/.quant-agent/
.env
.env.local
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
credentials*.json
secrets*.json
frontend/node_modules/
frontend/dist/
src-tauri/target/
src-tauri/binaries/qubit-*
src-tauri/resources/bundle/
dist/bundle/
python_connectors/__pycache__/
python_connectors/.venv/
# 预下载的 wheel 通常 100MB+，不进 git；发版时由打包流水线 / 用户本地脚本生成
python_connectors/wheels/*
!python_connectors/wheels/README.md
*.pyc

# Product / design / interview docs — local only (maintainer private)
# README screenshots remain public for the landing page.
docs/*
!docs/screenshots/
!docs/screenshots/**

# Private interview / job-search material (local only)
interview/
*.interview.md
.tmp-broker-test-home/

# IDE metadata
.idea/
.vscode/

# OS, logs, coverage, and local database sidecars
.DS_Store
*.log
coverage/
.nyc_output/
*.sqlite-shm
*.sqlite-wal
*.sqlite-journal
*.db
*.db-shm
*.db-wal
*.db-journal

# Python caches and local environments
**/__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Local reconstruction / scratch copies
.reconstructed-writes/

# Runtime workspace config (local)
.qubit/

# Bun compile build cache
.*.bun-build

# Pixel office font subset debug output
frontend/src/assets/fonts/.subset-chars.txt

# Pixel office raw art packs (downloaded from itch.io; CC-BY/CC0)
# 仅入库构建后的 atlas（themes/<id>/atlas.{png,json}）与 manifest.ts，
# 原始 zip 解压目录不入库。下载与解压步骤见：
#   frontend/src/assets/pixel-office/README.md
frontend/src/assets/pixel-office/raw/

# Agent runtime snapshots
.tmp-agent-snapshot-test/

# Agent readiness eval outputs (per-run JSON / MD reports, traces, run.log)
out/
