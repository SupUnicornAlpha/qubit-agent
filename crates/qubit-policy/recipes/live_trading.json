{
  "key": "live_trading",
  "aliases": ["paper_trading", "live_trading_short", "lt", "sim_trading"],
  "version": "2026-08-06.1",
  "capability_owners": {
    "strategy": "strategy",
    "order": "execution",
    "risk": "risk"
  },
  "role_tool_allowlist": {
    "orchestrator": ["update_plan", "topology.dispatch"],
    "execute": ["strategy.create_version", "order.create_intent"]
  },
  "stall_budget": {
    "tools": [
      "market.readiness",
      "market.resolve_symbol",
      "update_plan",
      "evaluate_risk",
      "strategy.create_version",
      "mcp:mathjs:evaluate"
    ],
    "key": "tool_fingerprint",
    "max_success": 1,
    "on_exceed": "strip_from_surface"
  },
  "recovery": {
    "after_probe_failure": "continue_without_realtime",
    "forbid_gap_as_final_answer": true
  },
  "completion": {
    "artifacts": [
      {
        "table": "order_intent",
        "min_rows": 1,
        "research_min_rows": 1,
        "required_fields": ["symbol", "side", "qty", "strategy_version_id"],
        "scope": "workflow"
      },
      {
        "table": "risk_decision",
        "min_rows": 1,
        "research_min_rows": 0,
        "required_fields": ["decision"],
        "scope": "workflow"
      }
    ],
    "required_tools": [{ "capability": "order.create_intent", "min_success": 1 }],
    "answer_schema": {
      "required_sections": ["goal", "evidence", "decision", "risks", "gaps"]
    }
  },
  "checklist_prompt": [
    "1. 若尚无 strategy_version_id：先 strategy.create_version（成功一次即可）",
    "2. 立刻调用 order.create_intent：默认 dispatch_mode=sim（富途等券商模拟盘）；本地假成交用 paper；真钱用 live+thesisId。sim 可省略 thesis；需启用的 sandbox 券商账户（或传 broker_account_id）",
    "3. risk_decision 由 order.create_intent 内嵌；不要先走券商 submit 或 rule.evaluate",
    "4. 输出五段答案；不得仅以行情不可用结案",
    "同类工具连打 ≤3 次后必须写终答；禁止 readiness/evaluate_risk 空转；TTFT：少工具、短观察、直接落单"
  ]
}
