# Core
numpy>=1.26.0
pandas>=2.2.0

# Technical analysis
# ta-lib>=0.4.28  # optional: requires system libta-lib

# Data sources (install as needed)
akshare>=1.12.0
# Optional: only required when klinesDataSource=yfinance (or fetch_dividends/
# fetch_earnings/fetch_asset_info on the qubit-data connector).
yfinance>=0.2.40
# tushare>=1.4.0
# WindPy — bundled with Wind Financial Terminal; copy from Wind install dir or:
#   pip install WindPy  (only works when Wind Terminal is installed on the machine)
# baostock>=0.8.8

# Backtest engines (install as needed)
# backtrader>=1.9.78
# vnpy>=3.9.0

# Broker (optional — user enables real trading via OpenD / IB Gateway / CCXT)
# futu-api>=9.0.0
# ib-insync>=0.9.86
# ccxt>=4.0.0

# Dev / test
pytest>=8.0.0
