-- Memory V2: document workspace_shared visibility (TEXT column — no DDL change required).
-- Recall layer routes workspace_shared to scope=workspace targets only.
-- See src/runtime/experience/recall-visibility.ts
SELECT 1;
